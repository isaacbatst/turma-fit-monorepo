version: '3.8'

services:
  db:
    extends:
      file: ./docker-compose.yml
      service: db
  api:
    extends:
      file: ./docker-compose.yml
      service: api
    build: 
      target: runner
  dashboard:
    extends:
      file: ./docker-compose.yml
      service: dashboard
    build: 
      target: runner
